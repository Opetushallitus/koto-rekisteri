{
  "name": "sendauditlogstokoski",
  "type": "module",
  "scripts": {
    "build": "esbuild src/handler.ts --bundle --platform=node --target=node20 --outfile=dist/index.js --sourcemap",
    "zip": "cd dist && zip -r ../function.zip ."
  },
  "devDependencies": {
    "esbuild": "^0.23.0",
    "typescript": "^5.5.0",
    "@types/aws-lambda": "^8.10.0"
  }
}
